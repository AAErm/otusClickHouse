# Для удобства сделал несколько скриптов

`make up` - поднимает образ Clickhouse

`make run` - поднимает образ и заходит в контейнер

`make logs` - просматривает логи контейнера

`make down` - удаляет контейнер